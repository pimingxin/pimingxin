
名称	类型	说明
INT	整型	4字节整数类型，范围约+/-21亿
BIGINT	长整型	8字节整数类型，范围约+/-922亿亿
REAL	浮点型	4字节浮点数，范围约+/-1038
DOUBLE	浮点型	8字节浮点数，范围约+/-10308
DECIMAL(M,N)	高精度小数	由用户指定精度的小数，例如，DECIMAL(20,10)表示一共20位，其中小数10位，通常用于财务计算
CHAR(N)	定长字符串	存储指定长度的字符串，例如，CHAR(100)总是存储100个字符的字符串
VARCHAR(N)	变长字符串	存储可变长度的字符串，例如，VARCHAR(100)可以存储0~100个字符的字符串
BOOLEAN	布尔类型	存储True或者False
DATE	日期类型	存储日期，例如，2018-06-22
TIME	时间类型	存储时间，例如，12:20:59
DATETIME	日期和时间类型	存储日期+时间，例如，2018-06-22 12:20:59


主键
	主键最好不能修改 不使用业务相关的字段作为主键 
	最好定义成 BIGINT NOT NULL AUTO_INCREMENT

外键
	通过外键的字段 可以把数据于另一张表关联起来 这种称为外键

多对多的关系

关系数据库通过外键 可以实现一对多 多对多 一对一的关系

索引
	对于主键 关系数据库会自动对其创建主键索引

语法
	查
	SELECT * FROM <表名>

	SELECT 1 测试数据库连接

	通过WHERE条件
	
	SELECT * FROM <表名> WHERE <表名内的行名> >= <表名行名的列中的数据>

	例如 SELECT * FROM students WHERE score >= 80

	WHERE     OR    AND   NOT