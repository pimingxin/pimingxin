思考：为什么可以用img，script，link随意加载跨域资源。但ajax不行。
最重要的区别在哪里？



因为用img script link加载的资源 只有原页面可以使用 
原页面的脚本无法获取新页面的内容 
浏览器认为这是安全的


而AJAX可以获取
浏览器认为这并不是安全的

跨域可以发送请求 但是拿不到响应

AJAX 可以拿到源代码并使用
script 拿不到源代码

script 信任那个地址加载的脚本 